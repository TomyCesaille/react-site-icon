---
phase: 06-demo-site
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - docs/src/components/Playground.tsx
autonomous: true
requirements: [DEMO-02]
user_setup: []

must_haves:
  truths:
    - "Pre-filled grid of 8 domains renders favicons on page load"
    - "User can type any domain and see its favicon appear in the grid"
    - "Code preview updates live as user changes domain, strategy, or size"
    - "Advanced panel is collapsed by default and expands on toggle"
    - "Strategy toggle and size slider affect all grid items and code preview"
    - "Fallback '?' renders for non-existent domains"
  artifacts:
    - path: "docs/src/components/Playground.tsx"
      provides: "Interactive playground React component with grid, input, code preview, advanced panel"
      exports: ["default"]
      min_lines: 100
  key_links:
    - from: "docs/src/components/Playground.tsx"
      to: "react-site-icon"
      via: "import { SiteIcon } from 'react-site-icon'"
      pattern: "import.*SiteIcon.*from.*react-site-icon"
    - from: "docs/src/pages/index.astro"
      to: "docs/src/components/Playground.tsx"
      via: "client:visible hydration"
      pattern: "Playground.*client:visible"
---

<objective>
Build the interactive Playground React component that is the demo site's centerpiece. It contains the domain input, pre-filled favicon grid, live-updating code preview with CSS-only syntax highlighting, and collapsible advanced panel with strategy toggle and size slider. This single React island is hydrated via client:visible by the index.astro page created in Plan 01.

Purpose: Delivers the core interactive experience (DEMO-02) -- visitors can test any domain and see live favicons before installing the library.

Output: Complete docs/src/components/Playground.tsx replacing the Plan 01 placeholder.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-demo-site/06-CONTEXT.md
@.planning/phases/06-demo-site/06-RESEARCH.md
@.planning/phases/06-demo-site/06-UI-SPEC.md

@src/SiteIcon.tsx
@docs/src/styles/global.css

<interfaces>
<!-- SiteIcon component -- imported from 'react-site-icon' via file:.. symlink -->
From src/SiteIcon.tsx:
```typescript
export interface SiteIconProps extends Omit<
  ComponentPropsWithoutRef<'img'>,
  'src' | 'width' | 'height' | 'onLoad' | 'onError'
> {
  domain: string;
  size?: number;             // default: 32
  fallback?: ReactNode;
  strategy?: 'lazy' | 'eager' | 'hidden';  // default: 'lazy'
  onResolved?: (found: boolean) => void;
}
```

<!-- CSS variables available from global.css (loaded by Layout.astro) -->
Available CSS variables:
- Colors: --bg, --bg-secondary, --text, --text-muted, --accent, --border, --code-bg
- Spacing: --space-xs (4px), --space-sm (8px), --space-md (16px), --space-lg (24px), --space-xl (32px)
- Font: --font-mono
- Token classes: .token-keyword, .token-string, .token-tag, .token-attr (defined in global.css)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build complete Playground React component</name>
  <files>docs/src/components/Playground.tsx</files>
  <read_first>
    docs/src/components/Playground.tsx,
    docs/src/styles/global.css,
    docs/src/pages/index.astro,
    src/SiteIcon.tsx,
    .planning/phases/06-demo-site/06-UI-SPEC.md
  </read_first>
  <action>
Replace the placeholder `docs/src/components/Playground.tsx` with the full interactive playground component.

**Imports:**
```tsx
import { useState } from 'react';
import { SiteIcon } from 'react-site-icon';
```

**Constants (per D-02, UI-SPEC Favicon Grid Contract):**
```tsx
const DEFAULT_DOMAINS = [
  'github.com',
  'google.com',
  'stackoverflow.com',
  'npmjs.com',
  'twitter.com',
  'reddit.com',
  'dev.to',
  'this-domain-does-not-exist-xyz.com',
];
```
The last domain intentionally triggers fallback to demonstrate the library's key feature.

**Component state (per D-05, D-06, D-07):**
- `domain: string` (initially `''`) -- user-typed domain from input
- `size: number` (initially `32`) -- favicon size from slider
- `strategy: 'lazy' | 'eager' | 'hidden'` (initially `'lazy'`) -- from select
- `showAdvanced: boolean` (initially `false`) -- advanced panel visibility

**Computed values:**
- `allDomains`: If `domain` is non-empty after trimming, prepend it to `DEFAULT_DOMAINS`. Otherwise just `DEFAULT_DOMAINS`.
- `displayDomain`: `domain.trim() || 'github.com'` -- used in code preview when input is empty

**JSX structure (all CSS uses inline styles referencing CSS variables via `var(--token)` since this is a React component, not Astro scoped):**

1. **Section wrapper**: `<section>` with styles: `background: var(--bg-secondary)`, `padding: var(--space-lg)`, `border-radius: 8px`, `border: 1px solid var(--border)`

2. **Playground heading**: `<h2>` with text "Try any domain" -- `font-family: var(--font-mono)`, `font-size: 20px`, `font-weight: 700`, `line-height: 1.2`, `color: var(--text)`, `margin: 0 0 var(--space-md) 0`

3. **Domain input** (per D-05, UI-SPEC Interaction Contract):
   - `<label htmlFor="domain-input" style={{ position: 'absolute', width: '1px', height: '1px', overflow: 'hidden', clip: 'rect(0,0,0,0)' }}>Domain to preview</label>` (visually hidden but accessible)
   - `<input id="domain-input" type="text" value={domain} onChange={(e) => setDomain(e.target.value)} placeholder="github.com, stackoverflow.com, any URL..." />`
   - Input styles: `width: '100%'`, `padding: '8px 16px'` (--space-sm block, --space-md inline), `border: '1px solid var(--border)'`, `borderRadius: '4px'`, `fontSize: '16px'`, `fontFamily: 'inherit'`, `color: 'var(--text)'`, `backgroundColor: 'var(--bg)'`, `outline: 'none'`
   - No debounce per UI-SPEC ("None. SiteIcon handles domain changes internally.")

4. **Favicon grid** (per D-02, UI-SPEC Favicon Grid Contract):
   - Container `<div>` with styles: `display: 'grid'`, `gridTemplateColumns: 'repeat(auto-fill, minmax(120px, 1fr))'`, `gap: 'var(--space-lg)'` (24px), `marginTop: 'var(--space-md)'`
   - Each grid item is a `<div key={d}>` with styles: `display: 'flex'`, `flexDirection: 'column'`, `alignItems: 'center'`, `gap: 'var(--space-xs)'`, `padding: 'var(--space-sm)'`
   - Favicon: `<SiteIcon domain={d} size={size} strategy={strategy} fallback={<span style={{ display: 'inline-flex', alignItems: 'center', justifyContent: 'center', width: size, height: size, fontSize: size, lineHeight: '1', color: 'var(--text-muted)' }} aria-hidden="true">?</span>} />`
   - Domain label: `<span style={{ fontSize: '14px', color: 'var(--text-muted)', maxWidth: '120px', overflow: 'hidden', textOverflow: 'ellipsis', whiteSpace: 'nowrap' }}>{d}</span>`

5. **Code preview** (per D-05, D-07, UI-SPEC Interaction Contract):
   - Container: `<pre style={{ background: 'var(--code-bg)', padding: 'var(--space-md)', borderRadius: '4px', border: '1px solid var(--border)', fontFamily: 'var(--font-mono)', fontSize: '14px', overflowX: 'auto', marginTop: 'var(--space-md)', lineHeight: '1.5' }} aria-label="Generated code example"><code>...</code></pre>`
   - Content uses JSX with `<span>` elements for CSS-only syntax highlighting per UI-SPEC Code Highlight Token Colors:
     - `.token-keyword` class for `import`, `from`
     - `.token-string` class for string literals (the module name, domain value)
     - `.token-tag` class for JSX tag names (`SiteIcon`, `span`)
     - `.token-attr` class for attribute names (`domain`, `size`, `strategy`, `fallback`)
   - Build the highlighted code as JSX elements. The code preview shows:
     ```
     import { SiteIcon } from 'react-site-icon';

     <SiteIcon
       domain="{displayDomain}"
       {size !== 32 ? `size={${size}}` : omitted}
       {strategy !== 'lazy' ? `strategy="${strategy}"` : omitted}
       fallback={<span>?</span>}
     />
     ```
   - Lines for `size` and `strategy` are only rendered when they differ from defaults (32 and 'lazy' respectively) per UI-SPEC.
   - Each line is a separate set of spans so highlighting is straightforward. Use `className="token-keyword"` etc. on the spans.

6. **Advanced toggle button** (per D-06, UI-SPEC Interaction Contract):
   - `<button onClick={() => setShowAdvanced(!showAdvanced)} aria-expanded={showAdvanced} aria-controls="advanced-panel">`
   - Text: `{showAdvanced ? 'Hide Advanced' : 'Show Advanced'}`
   - Styles: `background: 'none'`, `border: 'none'`, `color: 'var(--accent)'`, `cursor: 'pointer'`, `fontFamily: 'inherit'`, `fontSize: '14px'`, `padding: 'var(--space-sm) 0'`, `marginTop: 'var(--space-sm)'`

7. **Advanced panel** (per D-06, UI-SPEC Interaction Contract):
   - Wrapper `<div id="advanced-panel">` with animation styles: `maxHeight: showAdvanced ? '200px' : '0'`, `overflow: 'hidden'`, `transition: 'max-height 200ms ease-out'`
   - Inner content `<div>` with `padding: 'var(--space-md) 0'`, `display: 'flex'`, `gap: 'var(--space-lg)'`, `flexWrap: 'wrap'`, `alignItems: 'center'`
   - **Strategy select**: `<label style={{ display: 'flex', alignItems: 'center', gap: 'var(--space-sm)', fontSize: '14px', color: 'var(--text)' }}>Strategy: <select value={strategy} onChange={(e) => setStrategy(e.target.value as 'lazy' | 'eager' | 'hidden')} style={{ fontFamily: 'inherit', fontSize: '14px', padding: '4px 8px', border: '1px solid var(--border)', borderRadius: '4px', backgroundColor: 'var(--bg)', color: 'var(--text)' }}><option value="lazy">lazy</option><option value="eager">eager</option><option value="hidden">hidden</option></select></label>`
   - **Size slider**: `<label style={{ display: 'flex', alignItems: 'center', gap: 'var(--space-sm)', fontSize: '14px', color: 'var(--text)' }}>Size: {size}px <input type="range" min={16} max={128} step={1} value={size} onChange={(e) => setSize(Number(e.target.value))} style={{ accentColor: 'var(--accent)' }} /></label>`

**Export:**
```tsx
export default function Playground() { ... }
```

After creating the component, verify the full build chain works:
1. `npm run build` (library build -- produces dist/)
2. `npm run build --prefix docs` (Astro build -- imports from dist/ via symlink)
  </action>
  <verify>
    <automated>cd /Users/jorislacance/Documents/project/ReactThirdPartyDomainFavicon && npm run build && npm run build --prefix docs && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - docs/src/components/Playground.tsx contains `import { SiteIcon } from 'react-site-icon'`
    - docs/src/components/Playground.tsx contains `import { useState } from 'react'`
    - docs/src/components/Playground.tsx contains `export default function Playground`
    - docs/src/components/Playground.tsx contains `DEFAULT_DOMAINS` array with 8 domains
    - docs/src/components/Playground.tsx contains `'this-domain-does-not-exist-xyz.com'` (fallback demo)
    - docs/src/components/Playground.tsx contains `github.com` and `google.com` and `stackoverflow.com` in DEFAULT_DOMAINS
    - docs/src/components/Playground.tsx contains `useState` calls for domain, size, strategy, showAdvanced
    - docs/src/components/Playground.tsx contains `type="text"` input for domain
    - docs/src/components/Playground.tsx contains `placeholder` with "github.com"
    - docs/src/components/Playground.tsx contains `repeat(auto-fill, minmax(120px, 1fr))` (responsive grid)
    - docs/src/components/Playground.tsx contains `<SiteIcon` with `domain=` and `size=` and `strategy=` and `fallback=`
    - docs/src/components/Playground.tsx contains `aria-hidden="true"` on fallback span
    - docs/src/components/Playground.tsx contains `token-keyword` (syntax highlighting class)
    - docs/src/components/Playground.tsx contains `token-string` (syntax highlighting class)
    - docs/src/components/Playground.tsx contains `token-tag` (syntax highlighting class)
    - docs/src/components/Playground.tsx contains `token-attr` (syntax highlighting class)
    - docs/src/components/Playground.tsx contains `aria-expanded` on the advanced toggle button
    - docs/src/components/Playground.tsx contains `aria-controls="advanced-panel"` on the toggle button
    - docs/src/components/Playground.tsx contains `id="advanced-panel"` on the panel wrapper
    - docs/src/components/Playground.tsx contains `type="range"` (size slider)
    - docs/src/components/Playground.tsx contains `<select` with options lazy, eager, hidden
    - docs/src/components/Playground.tsx contains `max-height` or `maxHeight` for panel animation
    - `npm run build --prefix docs` exits 0 (Astro build succeeds with real Playground)
  </acceptance_criteria>
  <done>
    Playground component fully implements DEMO-02: domain input with live update, pre-filled grid of 8 domains showing real favicons (including one fallback demo), CSS-only syntax-highlighted code preview that updates live with domain/strategy/size changes, collapsible advanced panel with strategy select and size slider. Library and Astro builds both succeed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input -> SiteIcon domain prop | Domain string from text input passed to component |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-06-04 | Tampering | Domain input value | accept | React escapes all rendered strings by default. The domain value is passed to SiteIcon which normalizes via URL constructor. It is also displayed in code preview inside JSX `<span>` elements (React-escaped). No dangerouslySetInnerHTML used anywhere. |
| T-06-05 | Tampering | Code preview display | accept | Code preview is built with React JSX elements and spans, not innerHTML or template strings injected into DOM. React auto-escapes all interpolated values. |
</threat_model>

<verification>
1. `npm run docs:build` -- Full build chain succeeds (library + Astro)
2. `grep -q "import { SiteIcon } from 'react-site-icon'" docs/src/components/Playground.tsx` -- Imports from built package
3. `grep -q 'client:visible' docs/src/pages/index.astro` -- Hydration directive still present
4. `grep -c 'DEFAULT_DOMAINS' docs/src/components/Playground.tsx` -- Domain list defined
</verification>

<success_criteria>
- Playground component imports SiteIcon from 'react-site-icon' (not from ../src/)
- Pre-filled grid shows 8 domains including one that triggers fallback
- Domain input updates grid in real-time (no debounce)
- Code preview shows highlighted JSX that updates with current state
- Advanced panel collapses/expands with 200ms max-height transition
- Strategy select offers lazy/eager/hidden options
- Size slider ranges 16-128 with current value label
- Full build chain (npm run docs:build) succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/06-demo-site/06-02-SUMMARY.md`
</output>
