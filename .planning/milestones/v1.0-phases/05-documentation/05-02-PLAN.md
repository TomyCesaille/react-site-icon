---
phase: 05-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/basic/package.json
  - examples/basic/index.html
  - examples/basic/src/App.tsx
  - examples/basic/src/main.tsx
  - examples/basic/vite.config.ts
  - examples/basic/tsconfig.json
autonomous: true
requirements:
  - DOCS-05

must_haves:
  truths:
    - "The examples/basic/ directory contains a complete Vite+React project that StackBlitz can open"
    - "The example showcases 4-5 domains including one that triggers fallback rendering"
    - "The example uses only the default lazy strategy"
  artifacts:
    - path: "examples/basic/package.json"
      provides: "StackBlitz project manifest"
      contains: "react-site-icon"
    - path: "examples/basic/src/App.tsx"
      provides: "Multi-domain showcase component"
      contains: "SiteIcon"
    - path: "examples/basic/index.html"
      provides: "HTML entry point"
      contains: "html"
    - path: "examples/basic/vite.config.ts"
      provides: "Vite configuration"
      contains: "react"
    - path: "examples/basic/src/main.tsx"
      provides: "React entry point"
      contains: "createRoot"
  key_links:
    - from: "examples/basic/src/App.tsx"
      to: "react-site-icon"
      via: "npm import"
      pattern: "import.*SiteIcon.*react-site-icon"
    - from: "examples/basic/package.json"
      to: "react-site-icon"
      via: "dependency"
      pattern: "react-site-icon"
---

<objective>
Create a minimal Vite+React example project in examples/basic/ that StackBlitz can open directly from the GitHub repo URL, showcasing SiteIcon with multiple domains.

Purpose: Provide a one-click "try it now" experience per DOCS-05. The README links to `https://stackblitz.com/fork/github/jorislacance/react-site-icon/tree/main/examples/basic`.
Output: Complete examples/basic/ directory with package.json, Vite config, HTML entry, React entry, and App component.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-documentation/05-CONTEXT.md
@.planning/phases/05-documentation/05-RESEARCH.md

<interfaces>
<!-- SiteIcon props the example will use -->

From src/SiteIcon.tsx:
```typescript
export interface SiteIconProps extends Omit<
  ComponentPropsWithoutRef<'img'>,
  'src' | 'width' | 'height' | 'onLoad' | 'onError'
> {
  domain: string;
  size?: number;
  fallback?: ReactNode;
  strategy?: 'lazy' | 'eager' | 'hidden';
  onResolved?: (found: boolean) => void;
}
```

From src/index.ts:
```typescript
export { SiteIcon } from './SiteIcon';
export type { SiteIconProps } from './SiteIcon';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create examples/basic/ Vite+React project for StackBlitz</name>
  <files>
    examples/basic/package.json
    examples/basic/index.html
    examples/basic/src/App.tsx
    examples/basic/src/main.tsx
    examples/basic/vite.config.ts
    examples/basic/tsconfig.json
  </files>
  <read_first>
    - src/SiteIcon.tsx (component API to showcase — verify prop names and usage)
    - package.json (root package.json for React version reference and package name)
    - .planning/phases/05-documentation/05-CONTEXT.md (D-11: multi-domain showcase, D-12: default strategy only)
    - .planning/phases/05-documentation/05-RESEARCH.md (StackBlitz GitHub integration pattern, example directory structure)
  </read_first>
  <action>
Create the `examples/basic/` directory with these 6 files:

**1. examples/basic/package.json:**
```json
{
  "name": "react-site-icon-example",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-site-icon": "latest"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
```

Notes:
- `react-site-icon: "latest"` so StackBlitz always gets the published version
- Uses React 19 for the example (the library supports 17/18/19 but examples should use current)
- Minimal devDependencies — only what Vite+React needs

**2. examples/basic/index.html:**
```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>react-site-icon Example</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

**3. examples/basic/src/main.tsx:**
```tsx
import { StrictMode } from 'react';
import { createRoot } from 'react-dom/client';
import { App } from './App';

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <App />
  </StrictMode>,
);
```

**4. examples/basic/src/App.tsx:**
Create a multi-domain showcase component per D-11 and D-12.

Show 5 domains in a clean grid/list layout:
- github.com (well-known, always has a favicon)
- google.com (well-known, always has a favicon)
- stackoverflow.com (well-known, always has a favicon)
- npmjs.com (well-known, always has a favicon)
- this-domain-does-not-exist-xyz.com (triggers fallback — demonstrates detection)

Per D-12: Use ONLY the default lazy strategy. No strategy prop needed.

The component should:
- Import `{ SiteIcon }` from `'react-site-icon'`
- Render each domain with its name label next to the favicon
- Use a simple fallback like a grey circle or "?" span for the non-existent domain
- Include minimal inline styles for layout (no CSS file needed — keep it simple for StackBlitz)
- Include a brief heading explaining what the demo shows
- Optionally include an input field where users can type any domain to test live

Structure example:
```tsx
import { SiteIcon } from 'react-site-icon';
import { useState } from 'react';

const domains = [
  'github.com',
  'google.com',
  'stackoverflow.com',
  'npmjs.com',
  'this-domain-does-not-exist-xyz.com',
];

export function App() {
  const [custom, setCustom] = useState('');
  return (
    <div style={{ fontFamily: 'system-ui, sans-serif', padding: '2rem', maxWidth: 480 }}>
      <h1>react-site-icon</h1>
      <p>Display any website's favicon from its domain.</p>

      {domains.map((domain) => (
        <div key={domain} style={{ display: 'flex', alignItems: 'center', gap: '0.75rem', marginBottom: '0.75rem' }}>
          <SiteIcon
            domain={domain}
            size={32}
            fallback={<span style={{ display: 'inline-block', width: 32, height: 32, borderRadius: '50%', background: '#e5e7eb' }} />}
          />
          <code>{domain}</code>
        </div>
      ))}

      <hr style={{ margin: '1.5rem 0' }} />
      <h2>Try any domain</h2>
      <input
        type="text"
        value={custom}
        onChange={(e) => setCustom(e.target.value)}
        placeholder="Enter a domain..."
        style={{ padding: '0.5rem', fontSize: '1rem', width: '100%', marginBottom: '0.75rem' }}
      />
      {custom && (
        <div style={{ display: 'flex', alignItems: 'center', gap: '0.75rem' }}>
          <SiteIcon
            domain={custom}
            size={32}
            fallback={<span style={{ display: 'inline-block', width: 32, height: 32, borderRadius: '50%', background: '#e5e7eb' }} />}
          />
          <code>{custom}</code>
        </div>
      )}
    </div>
  );
}
```

**5. examples/basic/vite.config.ts:**
```ts
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
});
```

**6. examples/basic/tsconfig.json:**
```json
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "jsx": "react-jsx",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true
  },
  "include": ["src"]
}
```

IMPORTANT: Do NOT add `examples/` to the root package.json workspaces or files array. The example is standalone — StackBlitz installs its own dependencies.

Also ensure the root `.gitignore` does NOT exclude the examples directory. Check and update .gitignore if needed to allow examples/ to be committed.
  </action>
  <verify>
    <automated>test -f examples/basic/package.json && test -f examples/basic/src/App.tsx && test -f examples/basic/index.html && test -f examples/basic/vite.config.ts && test -f examples/basic/src/main.tsx && test -f examples/basic/tsconfig.json && grep -q "react-site-icon" examples/basic/package.json && grep -q "SiteIcon" examples/basic/src/App.tsx && grep -q "this-domain-does-not-exist" examples/basic/src/App.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - examples/basic/package.json exists and contains `"react-site-icon"` as a dependency
    - examples/basic/src/App.tsx exists and contains `import { SiteIcon } from 'react-site-icon'` (or similar named import)
    - examples/basic/src/App.tsx contains at least 4 real domains (github.com, google.com, etc.) and 1 non-existent domain for fallback demo
    - examples/basic/src/App.tsx does NOT contain `strategy=` prop (per D-12: default lazy only)
    - examples/basic/index.html exists with `<div id="root"></div>` and script tag
    - examples/basic/src/main.tsx exists with `createRoot` call
    - examples/basic/vite.config.ts exists with react plugin
    - examples/basic/tsconfig.json exists with `"jsx": "react-jsx"`
    - No changes to root package.json workspaces or files array
  </acceptance_criteria>
  <done>examples/basic/ contains a complete 6-file Vite+React project that StackBlitz can fork directly, showcasing 5 domains (4 real + 1 fallback trigger) with default lazy strategy only</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Example code is developer-facing, runs in StackBlitz sandbox or local dev server. No production deployment. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Information Disclosure | examples/basic/ | accept | Example contains no secrets. Uses only public domain names and the published npm package. |
| T-05-04 | Tampering | StackBlitz fork URL | accept | URL points to GitHub-hosted directory. Content controlled by repo owner. StackBlitz provides isolated sandbox. |
</threat_model>

<verification>
1. All 6 files exist in examples/basic/
2. package.json lists react-site-icon as dependency
3. App.tsx imports SiteIcon and renders 5 domains including fallback trigger
4. No strategy prop used (default lazy per D-12)
5. StackBlitz URL format works: https://stackblitz.com/fork/github/jorislacance/react-site-icon/tree/main/examples/basic
6. Root package.json not modified (no workspaces addition)
</verification>

<success_criteria>
- examples/basic/ is a standalone Vite+React project with 6 files
- Showcases SiteIcon with 5 domains (per D-11) using default strategy (per D-12)
- StackBlitz can open the project from the GitHub URL after repo push
- No modifications to root project configuration
</success_criteria>

<output>
After completion, create `.planning/phases/05-documentation/05-02-SUMMARY.md`
</output>
