---
phase: 05-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - package.json
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
  - DOCS-03
  - DOCS-04
  - DOCS-05
  - DOCS-06

must_haves:
  truths:
    - "README opens with package name, one-line description, 5 badges, and quick usage example visible without scrolling"
    - "A developer can find the full props table with all 6 SiteIcon props documented"
    - "A developer can see a comparison table with react-site-icon vs 4 alternatives"
    - "A developer can understand the naturalWidth detection technique from the Why section and ASCII diagram"
    - "A developer can click a StackBlitz link in the README"
    - "npm search 'favicon react' returns the package due to optimized keywords"
  artifacts:
    - path: "README.md"
      provides: "Complete library documentation"
      contains: "react-site-icon"
    - path: "package.json"
      provides: "npm search keywords"
      contains: "favicon"
  key_links:
    - from: "README.md"
      to: "src/SiteIcon.tsx"
      via: "Props table matches SiteIconProps interface"
      pattern: "domain.*string.*required"
    - from: "README.md"
      to: "package.json"
      via: "Badge URLs reference correct package name"
      pattern: "react-site-icon"
---

<objective>
Write the complete README.md for react-site-icon following cognitive funneling structure, and verify package.json keywords are optimized for npm search.

Purpose: Convert npm browsers into users within 60 seconds by presenting the library's value proposition, API, and differentiation clearly.
Output: README.md with badges, examples, API table, strategy guide, comparison table, StackBlitz link, and contributing/license sections. Updated package.json keywords if needed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-documentation/05-CONTEXT.md
@.planning/phases/05-documentation/05-RESEARCH.md

<interfaces>
<!-- Source of truth for props table — executor MUST read this file and match exactly -->

From src/SiteIcon.tsx (lines 10-24):
```typescript
export interface SiteIconProps extends Omit<
  ComponentPropsWithoutRef<'img'>,
  'src' | 'width' | 'height' | 'onLoad' | 'onError'
> {
  /** Domain to fetch the favicon for (e.g. "github.com" or "https://github.com/user/repo") */
  domain: string;
  /** Requested favicon size in pixels (default: 32) */
  size?: number;
  /** Content to render when no favicon is available */
  fallback?: ReactNode;
  /** Detection strategy: "lazy" shows fallback during detection, "eager" shows img immediately, "hidden" shows sized placeholder (default: "lazy") */
  strategy?: 'lazy' | 'eager' | 'hidden';
  /** Called when detection completes. `true` if favicon found, `false` if globe detected or error. Memoize if you don't want re-fires on re-render. */
  onResolved?: (found: boolean) => void;
}
```

From src/index.ts:
```typescript
export { SiteIcon } from './SiteIcon';
export type { SiteIconProps } from './SiteIcon';
```

From package.json:
- name: "react-site-icon"
- description: "A zero-dependency React component that displays any website's favicon from its domain name"
- keywords: ["favicon", "site-icon", "website-icon", "react", "react-component", "domain-favicon", "google-favicon"]
- repository: "https://github.com/jorislacance/react-site-icon.git"
- homepage: "https://jorislacance.github.io/react-site-icon/"
- license: "MIT"
- author: "Joris Lacance"

GitHub workflows: ci.yml, release.yml, deploy.yml
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write README.md with full cognitive funneling structure</name>
  <files>README.md</files>
  <read_first>
    - src/SiteIcon.tsx (canonical prop types — API table MUST match lines 10-24 exactly)
    - package.json (package name, repository URL, keywords for badge URLs)
    - .github/workflows/ci.yml (workflow filename for CI badge)
    - .planning/phases/05-documentation/05-CONTEXT.md (locked decisions D-01 through D-14)
    - .planning/phases/05-documentation/05-RESEARCH.md (badge URLs, comparison data, StackBlitz patterns)
  </read_first>
  <action>
Create README.md with the following exact section structure (per D-04):

**1. Package name (h1):** `# react-site-icon`

**2. One-line description:** A short, punchy line. Example: "Display any website's favicon from its domain. Zero dependencies. < 1KB."

**3. Badges (per D-13, D-14 order: npm, size, TypeScript, license, CI):**
```markdown
[![npm](https://img.shields.io/npm/v/react-site-icon)](https://www.npmjs.com/package/react-site-icon)
[![bundle size](https://img.shields.io/bundlephobia/minzip/react-site-icon)](https://bundlephobia.com/package/react-site-icon)
[![TypeScript](https://img.shields.io/badge/TypeScript-3178C6?logo=typescript&logoColor=fff)](#)
[![license](https://img.shields.io/npm/l/react-site-icon)](https://github.com/jorislacance/react-site-icon/blob/main/LICENSE)
[![CI](https://img.shields.io/github/actions/workflow/status/jorislacance/react-site-icon/ci.yml?branch=main)](https://github.com/jorislacance/react-site-icon/actions/workflows/ci.yml)
```

**4. Quick example (per D-03) — two snippets:**

Bare minimum (3 lines):
```tsx
import { SiteIcon } from 'react-site-icon';

<SiteIcon domain="github.com" />
```

With fallback (5 lines):
```tsx
<SiteIcon
  domain="example.com"
  fallback={<span>?</span>}
/>
```

**5. Why this library (per D-02) — the EXCEPTION to brevity:**
- Detailed explanation of the naturalWidth detection technique
- ASCII flow diagram showing: component renders -> Google CDN request (single request) -> two outcomes (real favicon at requested size OR 16x16 default globe) -> naturalWidth check -> show favicon or fallback
- Explain why other approaches fail: direct fetch has CORS issues, canvas comparison requires CORS headers Google doesn't send, target domain fetch is unreliable
- Use this exact ASCII diagram structure:
```
                    react-site-icon
                         |
           Google faviconV2 CDN request
                  (single request)
                         |
                   +-----+-----+
                   |           |
              Real favicon   Default globe
              (64x64 px)    (always 16x16)
                   |           |
           naturalWidth > 16  naturalWidth = 16
                   |           |
              Show <img>   Show fallback
```
- Below the diagram, briefly explain: Google's CDN returns a 16x16 default globe for unknown domains regardless of requested size. Checking naturalWidth after onLoad detects this without CORS issues.

**6. Install:**
```bash
npm install react-site-icon
```

**7. API — Props table (per D-06, DOCS-06):**
Table MUST match SiteIconProps exactly. Columns: Prop, Type, Default, Description.

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `domain` | `string` | required | Domain to fetch the favicon for (e.g. `"github.com"` or `"https://github.com/user/repo"`) |
| `size` | `number` | `32` | Requested favicon size in pixels |
| `fallback` | `ReactNode` | `null` | Content to render when no favicon is available |
| `strategy` | `'lazy' \| 'eager' \| 'hidden'` | `'lazy'` | Detection strategy (see Strategies below) |
| `onResolved` | `(found: boolean) => void` | — | Called when detection completes. `true` = found, `false` = globe/error |

After the table, note: "All standard `<img>` props (`className`, `style`, `alt`, `loading`, `decoding`, `data-*`, `aria-*`) are spread onto the underlying element. `src`, `width`, `height`, `onLoad`, `onError` are reserved."

**8. Strategies (per D-06) — dedicated section with:**
- Code example for each of the 3 strategies:
  - lazy (default): shows fallback during detection, swaps to favicon when found
  - eager: shows img immediately, may flash Google's default globe briefly
  - hidden: sized placeholder during detection, prevents layout shift
- Selection table: When to use which strategy

| Strategy | During detection | Best for |
|----------|-----------------|----------|
| `lazy` | Shows fallback | Lists with loading states |
| `eager` | Shows `<img>` directly | Above-the-fold content, fastest paint |
| `hidden` | Sized empty placeholder | Preventing layout shift |

**9. Advanced (per D-07) — grouped section with 2-3 short examples:**
- SSR behavior note: renders fallback on server, detects on client after hydration
- Ref forwarding example: `const imgRef = useRef<HTMLImageElement>(null); <SiteIcon ref={imgRef} domain="github.com" />`
- onResolved callback example: tracking favicon availability with useState

**10. Compare (per D-08, D-09, D-10) — comparison table vs 4 alternatives:**

| Feature | react-site-icon | favicon-stealer | DIY Google CDN | DIY domain fetch | Proxy services |
|---------|----------------|-----------------|----------------|------------------|----------------|
| Bundle size | < 1KB | ~3.5KB | 0 | 0 | 0 |
| Dependencies | 0 | 2 (React as dep!) | 0 | 0 | 0 |
| Fallback detection | Yes | No | No | No | No |
| Network requests | 1 | 1-2 | 1 | 1+ (may fail) | 1 |
| React versions | 17, 18, 19 | 19 only | Any | Any | Any |
| SSR compatible | Yes | Unknown | Manual | Manual | Manual |
| TypeScript | Full | Full | Manual | Manual | Manual |

Use checkmarks/crosses for boolean features per D-10. Use exact numbers where meaningful per D-10.

**11. StackBlitz link (per DOCS-05):**
Add a "Try it" section or inline link:
```markdown
[Try it on StackBlitz](https://stackblitz.com/fork/github/jorislacance/react-site-icon/tree/main/examples/basic)
```
This can be placed after the Quick Example section or as a standalone section. Position it where it provides the best flow — Claude's discretion.

**12. Contributing (per D-05) — brief inline section:**
```
git clone https://github.com/jorislacance/react-site-icon.git
cd react-site-icon
npm install
npm test
```
"PRs welcome."

**13. License:** "MIT" with link to LICENSE file.

**Tone (per D-01):** Developer-concise. Terse, code-forward, minimal prose. Let code examples and the comparison table do the talking. Think zustand, zod, nanoid READMEs. The "Why" section (per D-02) is the single exception where detailed explanation is appropriate.

**Code example format (Claude's discretion):** Use TSX for examples that show type annotations (onResolved, ref). Use JSX-style for simple examples (quick start). Import style: named import `{ SiteIcon }`.
  </action>
  <verify>
    <automated>grep -c "^#" README.md && grep -q "react-site-icon" README.md && grep -q "img.shields.io" README.md && grep -q "naturalWidth" README.md && grep -q "favicon-stealer" README.md && grep -q "stackblitz.com" README.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - README.md exists at project root
    - README.md line 1 is `# react-site-icon`
    - README.md contains exactly 5 badge image links: `img.shields.io/npm/v/react-site-icon`, `img.shields.io/bundlephobia/minzip/react-site-icon`, `img.shields.io/badge/TypeScript`, `img.shields.io/npm/l/react-site-icon`, `img.shields.io/github/actions/workflow/status/jorislacance/react-site-icon/ci.yml`
    - README.md contains `import { SiteIcon } from 'react-site-icon'` (quick example)
    - README.md contains `## Why` section with `naturalWidth` and ASCII diagram containing `+-----+-----+`
    - README.md contains `## API` section with props table containing `domain`, `size`, `fallback`, `strategy`, `onResolved`
    - README.md contains `## Strategies` section with `lazy`, `eager`, `hidden` code examples
    - README.md contains `## Advanced` section with `useRef` and `onResolved` examples
    - README.md contains comparison table with columns for `react-site-icon`, `favicon-stealer`, `DIY Google CDN`, `DIY domain fetch`
    - README.md contains `## Contributing` with `git clone https://github.com/jorislacance/react-site-icon.git`
    - README.md contains `## License` with `MIT`
    - README.md contains StackBlitz link `stackblitz.com/fork/github/jorislacance/react-site-icon`
    - Section order in README: name -> badges -> example -> Why -> Install -> API -> Strategies -> Advanced -> Compare -> Contributing -> License (per D-04)
  </acceptance_criteria>
  <done>README.md contains all 12 sections in the locked order, with 5 badges, props table matching SiteIconProps, 3 strategy examples, comparison table vs 4 alternatives, ASCII detection diagram, StackBlitz link, and inline contributing section</done>
</task>

<task type="auto">
  <name>Task 2: Verify and optimize package.json keywords for npm search</name>
  <files>package.json</files>
  <read_first>
    - package.json (current keywords array)
    - .planning/phases/05-documentation/05-RESEARCH.md (keyword analysis and npm search algorithm notes)
    - .planning/phases/05-documentation/05-CONTEXT.md (DOCS-03 requirement)
  </read_first>
  <action>
Read package.json and verify the keywords array contains exactly these 7 keywords required by DOCS-03:
`["favicon", "site-icon", "website-icon", "react", "react-component", "domain-favicon", "google-favicon"]`

The RESEARCH.md confirms these 7 keywords match the DOCS-03 spec exactly and cover:
- Primary function: favicon, site-icon, website-icon, domain-favicon
- Ecosystem: react, react-component
- Implementation detail: google-favicon

The research suggests two optional additions (`website-favicon`, `favicon-component`) but states "no changes required per DOCS-03". Since the current keywords already match the requirement spec, only add the two extras if they don't bloat the list unreasonably. The npm Algolia search indexes name + description + keywords, so more relevant keywords = more discoverable.

If keywords already match DOCS-03, this task is a verification-only pass. If any of the 7 required keywords are missing, add them.

Additionally, verify the `description` field is present and search-optimized. Current value: `"A zero-dependency React component that displays any website's favicon from its domain name"` — this is confirmed good by research (contains "React component", "favicon", "domain", "zero-dependency").
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); const k=['favicon','site-icon','website-icon','react','react-component','domain-favicon','google-favicon']; const ok=k.every(w=>p.keywords.includes(w)); console.log(ok ? 'PASS: all 7 DOCS-03 keywords present' : 'FAIL: missing keywords'); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - package.json `keywords` array contains at minimum: "favicon", "site-icon", "website-icon", "react", "react-component", "domain-favicon", "google-favicon"
    - package.json `description` field is non-empty and contains "React" and "favicon"
    - No unrelated or spammy keywords added
  </acceptance_criteria>
  <done>package.json keywords contain all 7 DOCS-03 required terms, description is search-optimized</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This phase creates documentation only (markdown files, package.json metadata). No security-relevant code paths are introduced.

| Boundary | Description |
|----------|-------------|
| None | Documentation-only phase — no runtime code changes, no new dependencies, no user input processing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | README.md badge URLs | accept | Badge URLs reference public GitHub repo and npm package name — no secrets exposed. All URLs use HTTPS. |
| T-05-02 | Tampering | StackBlitz link | accept | Link points to GitHub-hosted examples/ directory. User controls the repo content. StackBlitz runs in sandboxed browser environment. |
</threat_model>

<verification>
1. README.md renders correctly on GitHub (section headings, code blocks, tables, badges, links)
2. All 5 badge URLs use correct repository path `jorislacance/react-site-icon` and package name `react-site-icon`
3. Props table matches SiteIconProps interface in src/SiteIcon.tsx exactly (5 named props + rest props note)
4. Section order matches D-04: name -> badges -> example -> Why -> Install -> API -> Strategies -> Advanced -> Compare -> Contributing -> License
5. package.json keywords contain all 7 DOCS-03 required terms
6. npm test still passes (no source code regressions)
</verification>

<success_criteria>
- README.md exists with cognitive funneling structure and all 12 sections
- 5 shields.io badges with correct URLs render inline
- Props table documents all SiteIcon props accurately
- Comparison table covers 4 alternatives with verified data
- StackBlitz link present pointing to examples/basic/
- package.json keywords optimized for npm search
- npm test passes (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/05-documentation/05-01-SUMMARY.md`
</output>
