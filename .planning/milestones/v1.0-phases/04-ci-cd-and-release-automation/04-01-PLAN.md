---
phase: 04-ci-cd-and-release-automation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .changeset/config.json
  - LICENSE
autonomous: true
requirements:
  - CICD-04

must_haves:
  truths:
    - "package.json repository.url contains jorislacance/react-site-icon"
    - "package.json author field is Joris Lacance"
    - "package.json has homepage and bugs.url fields pointing to correct GitHub URLs"
    - "package.json has a release script that builds then publishes via changesets"
    - ".changeset/config.json repo field is jorislacance/react-site-icon"
    - "LICENSE file contains Copyright (c) 2026 Joris Lacance"
  artifacts:
    - path: "package.json"
      provides: "Repository identity, author, homepage, bugs, release script"
      contains: "jorislacance/react-site-icon"
    - path: ".changeset/config.json"
      provides: "Correct repo reference for changelog links"
      contains: "jorislacance/react-site-icon"
    - path: "LICENSE"
      provides: "MIT license with correct copyright holder"
      contains: "Copyright (c) 2026 Joris Lacance"
  key_links:
    - from: "package.json"
      to: ".changeset/config.json"
      via: "repo identity must match for changelog PR links"
      pattern: "jorislacance/react-site-icon"
    - from: "package.json scripts.release"
      to: ".github/workflows/release.yml"
      via: "release.yml calls npm run release"
      pattern: "npm run build && changeset publish"
---

<objective>
Fix repository identity placeholders and prepare release infrastructure in package.json, .changeset/config.json, and LICENSE.

Purpose: The release workflow (Plan 02) depends on correct repository identity for OIDC Trusted Publishing (npm matches the repository URL exactly) and a `release` script in package.json for changesets/action to invoke. The LICENSE file needs the author name for a complete open-source package.

Output: Updated package.json (identity fields + release script), updated .changeset/config.json (repo field), updated LICENSE (copyright holder).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ci-cd-and-release-automation/04-CONTEXT.md
@.planning/phases/04-ci-cd-and-release-automation/04-RESEARCH.md

<interfaces>
<!-- Existing configuration files that will be modified -->

From package.json (current state):
- `"repository": { "url": "https://github.com/OWNER/react-site-icon.git" }` -- placeholder to fix
- `"author": ""` -- empty, needs value
- No `homepage` or `bugs` fields -- need adding
- No `release` script -- need adding
- Existing scripts: lint, typecheck, test, test:exports, format:check, build

From .changeset/config.json (current state):
- `"changelog": ["@changesets/changelog-github", { "repo": "OWNER/react-site-icon" }]` -- placeholder to fix

From LICENSE (current state):
- `Copyright (c) 2026` -- missing author name
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix repository identity in package.json and .changeset/config.json</name>
  <files>package.json, .changeset/config.json</files>
  <read_first>package.json, .changeset/config.json</read_first>
  <action>
Update package.json with the following changes (per D-19, D-20, D-21, D-22):

1. Change `"repository.url"` from `"https://github.com/OWNER/react-site-icon.git"` to `"https://github.com/jorislacance/react-site-icon.git"` (per D-19)

2. Change `"author"` from `""` to `"Joris Lacance"` (per D-20)

3. Add `"homepage"` field with value `"https://jorislacance.github.io/react-site-icon/"` (per D-21). Place it after the `"license"` field.

4. Add `"bugs"` object with `"url"` field: `"bugs": { "url": "https://github.com/jorislacance/react-site-icon/issues" }` (per D-22). Place it after the `"repository"` field.

5. Add `"release"` script: `"release": "npm run build && changeset publish"` (per D-01, required by changesets/action). Add it after the existing `"test:exports"` script. IMPORTANT: The script name MUST be `release`, NOT `publish` -- npm has a built-in `publish` lifecycle that would conflict.

Update .changeset/config.json (per D-19):

1. Change the `"repo"` value in the changelog array from `"OWNER/react-site-icon"` to `"jorislacance/react-site-icon"`
  </action>
  <verify>
    <automated>grep -q '"url": "https://github.com/jorislacance/react-site-icon.git"' package.json && grep -q '"author": "Joris Lacance"' package.json && grep -q '"homepage": "https://jorislacance.github.io/react-site-icon/"' package.json && grep -q '"url": "https://github.com/jorislacance/react-site-icon/issues"' package.json && grep -q '"release": "npm run build && changeset publish"' package.json && grep -q '"repo": "jorislacance/react-site-icon"' .changeset/config.json && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- package.json contains `"url": "https://github.com/jorislacance/react-site-icon.git"` in repository
- package.json contains `"author": "Joris Lacance"`
- package.json contains `"homepage": "https://jorislacance.github.io/react-site-icon/"`
- package.json contains `"bugs": { "url": "https://github.com/jorislacance/react-site-icon/issues" }`
- package.json contains `"release": "npm run build && changeset publish"` in scripts
- package.json does NOT contain the string `OWNER`
- .changeset/config.json contains `"repo": "jorislacance/react-site-icon"`
- .changeset/config.json does NOT contain the string `OWNER`
- `npm run lint` still passes (no regressions)
- `npm run build` still passes (no regressions)
  </acceptance_criteria>
  <done>package.json has correct repository URL, author, homepage, bugs.url, and release script. .changeset/config.json has correct repo field. No OWNER placeholders remain in either file.</done>
</task>

<task type="auto">
  <name>Task 2: Update LICENSE copyright holder</name>
  <files>LICENSE</files>
  <read_first>LICENSE</read_first>
  <action>
Update the LICENSE file copyright line (per D-23):

Change line 3 from:
```
Copyright (c) 2026
```
to:
```
Copyright (c) 2026 Joris Lacance
```

Do NOT change any other content in the file. The rest of the MIT license text remains identical.
  </action>
  <verify>
    <automated>grep -q "Copyright (c) 2026 Joris Lacance" LICENSE && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- LICENSE file line 3 reads exactly `Copyright (c) 2026 Joris Lacance`
- The rest of the MIT license text is unchanged (21 lines total)
- No other modifications to the file
  </acceptance_criteria>
  <done>LICENSE file has complete copyright line with author name per D-23.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package.json -> npm registry | Repository URL must match OIDC trusted publisher config exactly |
| .changeset/config.json -> GitHub API | Repo field used by @changesets/changelog-github to create PR links |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | package.json author/homepage | accept | Author name and GitHub URL are intentionally public for an open-source project |
| T-04-02 | Tampering | package.json repository.url mismatch with npm OIDC config | mitigate | Verify URL matches exactly: `jorislacance/react-site-icon.git` -- OIDC publish fails on mismatch |
</threat_model>

<verification>
```bash
# Verify no OWNER placeholders remain
! grep -r "OWNER" package.json .changeset/config.json

# Verify release script exists and works
npm run --silent release --dry-run 2>&1 || true  # Just verify script is recognized

# Verify existing checks still pass
npm run lint && npm run build
```
</verification>

<success_criteria>
- All OWNER placeholders replaced with jorislacance in both files
- package.json has author, homepage, bugs, and release script
- LICENSE has complete copyright holder
- Existing lint and build commands still pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-ci-cd-and-release-automation/04-01-SUMMARY.md`
</output>
